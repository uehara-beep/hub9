<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
  <title>HUB9</title>
  <style>
    :root{
      --bg:#f7f3ee;
      --card:#ffffff;
      --text:#1f1f1f;
      --muted:rgba(0,0,0,.55);
      --hermes:#ff7a00;
      --radius:18px;
    }
    body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,"Apple Color Emoji","Segoe UI Emoji";}
    .wrap{max-width:720px;margin:0 auto;padding:14px 14px 90px;}
    .card{background:var(--card);border-radius:var(--radius);box-shadow:0 10px 30px rgba(0,0,0,.06);padding:14px;}
    .row{display:flex;gap:10px;align-items:center;}
    .muted{color:var(--muted);font-size:13px;}
    .btn{display:inline-flex;align-items:center;justify-content:center;border-radius:14px;padding:12px 14px;border:0;cursor:pointer;touch-action:manipulation;}
    .btn-primary{background:var(--hermes);color:#fff;box-shadow:0 10px 24px rgba(0,0,0,.12);font-weight:700;}
    .nav{position:fixed;left:0;right:0;bottom:0;padding:10px 12px calc(10px + env(safe-area-inset-bottom));background:rgba(255,255,255,.78);backdrop-filter: blur(10px);border-top:1px solid rgba(0,0,0,.06);}
    .nav-inner{max-width:720px;margin:0 auto;display:flex;gap:10px;justify-content:space-between;}
    .nav a{flex:1;text-align:center;text-decoration:none;color:rgba(0,0,0,.75);font-size:12px;padding:10px 8px;border-radius:16px;}
    .nav a:active{transform:scale(.99);}
    .nav a strong{display:block;font-size:18px;line-height:18px;margin-bottom:4px;}
    .banner{border:1px solid rgba(255,122,0,.25);background:rgba(255,122,0,.10);border-radius:16px;padding:10px 12px;margin-bottom:10px;}
    .hide{display:none;}
  </style>
</head>
<body>
  <div class="wrap">
    <% latest = Notification.where(read_at: nil).order(created_at: :desc).first rescue nil %>
    <% if latest %>
      <div class="banner">
        <div class="row" style="justify-content:space-between;">
          <div>
            <div style="font-weight:800;">通知</div>
            <div class="muted"><%= latest.title %></div>
          </div>
          <a href="/notifications" class="btn" style="background:#fff;border:1px solid rgba(0,0,0,.08);">見る</a>
        </div>
      </div>
    <% end %>

    <div class="card">
      <div class="row" style="justify-content:space-between;">
        <div>
          <div style="font-weight:900;font-size:20px;letter-spacing:.2px;">HUB9</div>
          <div class="muted">Ta9専用・秘書寄り（ここから育てる）</div>
        </div>
        <button class="btn btn-primary" id="horseBtn" aria-label="horse">🐴</button>
      </div>

      <div style="margin-top:12px;">
        <div class="muted">まずはここ：</div>
        <div style="display:flex;gap:10px;margin-top:10px;flex-wrap:wrap;">
          <a class="btn" href="/notifications" style="background:#fff;border:1px solid rgba(0,0,0,.08);">通知</a>
          <a class="btn" href="/vault" style="background:#fff;border:1px solid rgba(0,0,0,.08);">B3</a>
        </div>
      </div>
    </div>

    <div style="height:12px;"></div>

    <div class="card">
      <div style="font-weight:800;">今日の一手</div>
      <div class="muted" style="margin-top:6px;">「領収書」→「OCR」→「記録」 を最短で回す</div>
      <div style="margin-top:12px;display:flex;gap:10px;flex-wrap:wrap;">
        <a class="btn btn-primary" href="/vault" style="text-decoration:none;">B3へ</a>
        <a class="btn" href="/notifications" style="background:#fff;border:1px solid rgba(0,0,0,.08);text-decoration:none;">通知</a>
      </div>
    </div>
  </div>

  <div class="nav">
    <div class="nav-inner">
      <a href="/" aria-label="home"><strong>🏠</strong>Home</a>
      <a href="/vault" aria-label="ledger"><strong>🧾</strong>記録</a>
      <a href="/vault" aria-label="receipt"><strong>📸</strong>レシート</a>
      <a href="/vault" aria-label="card"><strong>💳</strong>名刺</a>
      <a href="/notifications" aria-label="settings"><strong>⚙️</strong>設定</a>
    </div>
  </div>

  <script>
    // 入口を隠したい場合：horseBtnを長押しで /vault に遷移、などに変更可
    document.getElementById("horseBtn").addEventListener("click", () => {
      window.location.href = "/vault";
    });
  </script>
</body>
</html>
