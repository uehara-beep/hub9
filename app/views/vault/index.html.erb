<div class="vault-shell vault-bg">
  <div class="vault-stage">
    <div class="vault-top">
      <h1>🐴 Vault</h1>
      <div class="pin-hint">外に出せない記録（へそくり/領収書）</div>
    </div>

    <div class="vault-actions vault-card pin-wrap"
         data-controller="vault-pin"
         data-vault-pin-length-value="4">
      <div class="pin-title">PIN</div>

      <div class="pin-dots" aria-label="PIN dots">
        <div class="pin-dot" data-vault-pin-target="dot"></div>
        <div class="pin-dot" data-vault-pin-target="dot"></div>
        <div class="pin-dot" data-vault-pin-target="dot"></div>
        <div class="pin-dot" data-vault-pin-target="dot"></div>
      </div>

      <input type="password"
             inputmode="numeric"
             autocomplete="one-time-code"
             class="sr-only"
             data-vault-pin-target="input" />

      <div class="pin-pad">
        <% (1..9).each do |n| %>
          <button type="button"
                  class="pin-btn"
                  data-action="click->vault-pin#press"
                  data-digit="<%= n %>"><%= n %></button>
        <% end %>

        <button type="button"
                class="pin-btn ghost"
                data-action="click->vault-pin#backspace">⌫</button>

        <button type="button"
                class="pin-btn"
                data-action="click->vault-pin#press"
                data-digit="0">0</button>

        <button type="button"
                class="pin-btn primary"
                data-action="click->vault-pin#submit">OK</button>
      </div>

      <div class="pin-hint">数字をタップ → OK</div>

      <% if flash[:alert].present? %>
        <div class="vault-alert"><%= flash[:alert] %></div>
      <% end %>
    </div>
  </div>
</div>
