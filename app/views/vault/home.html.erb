<div class="vault-page">
  <div class="vault-hero">
    <h1>🐴 Vault</h1>
    <p>外に出せない記録（へそくり/領収書）</p>
  </div>

  <div class="vault-actions">
    <%= link_to "/vault/transfer", class: "vault-card" do %>
      💸 送金（貰い/支払い/立替）
    <% end %>

    <%= link_to "/vault/entries?hidden=1", class: "vault-card" do %>
      💴 裏金メモ
    <% end %>

    <%= link_to "/vault/entries/new?kind=out&hidden=1", class: "vault-card" do %>
      🧾 領収書（写メ）
    <% end %>
  </div>
</div>
