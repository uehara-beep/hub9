<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <meta name="csrf-token" content="<%= form_authenticity_token %>">
  <title>HUB9 - <%= @modes.find{|x| x[:key]==@mode}&.dig(:label) || "秘書" %></title>
  <style>
    :root {
      --bg: #f6f3ef;
      --card: #fff;
      --text: #1a1a1a;
      --muted: #666;
      --hermes: #ff7a00;
      --user-bg: #ff7a00;
      --user-text: #fff;
      --ai-bg: #fff;
      --ai-text: #1a1a1a;
      --safe-top: env(safe-area-inset-top, 0px);
      --safe-bottom: env(safe-area-inset-bottom, 0px);
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html, body {
      height: 100%;
      overflow: hidden;
    }
    body {
      background: var(--bg);
      color: var(--text);
      font-family: -apple-system, BlinkMacSystemFont, "Hiragino Sans", sans-serif;
      display: flex;
      flex-direction: column;
    }

    /* Fixed Header */
    .chat-header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      background: var(--hermes);
      color: #fff;
      padding: calc(12px + var(--safe-top)) 16px 12px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .chat-header-left {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .chat-back {
      color: #fff;
      text-decoration: none;
      font-size: 24px;
      padding: 4px;
    }
    .chat-title {
      font-size: 18px;
      font-weight: 800;
    }
    .chat-badge {
      background: rgba(255,255,255,.2);
      padding: 4px 10px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 600;
    }

    /* Scrollable Chat Area */
    .chat-messages {
      flex: 1;
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
      padding: calc(70px + var(--safe-top)) 16px calc(80px + var(--safe-bottom));
      display: flex;
      flex-direction: column-reverse; /* 新しいメッセージが上 */
      gap: 12px;
    }

    /* Message Bubbles */
    .bubble {
      max-width: 85%;
      padding: 12px 16px;
      border-radius: 18px;
      font-size: 15px;
      line-height: 1.5;
      word-break: break-word;
    }
    .bubble.user {
      align-self: flex-end;
      background: var(--user-bg);
      color: var(--user-text);
      border-bottom-right-radius: 4px;
    }
    .bubble.assistant {
      align-self: flex-start;
      background: var(--ai-bg);
      color: var(--ai-text);
      border-bottom-left-radius: 4px;
      box-shadow: 0 2px 8px rgba(0,0,0,.06);
    }
    .bubble-role {
      font-size: 11px;
      opacity: .6;
      margin-bottom: 4px;
      text-transform: uppercase;
    }
    .bubble-text p {
      margin: 0;
    }
    .bubble-text p + p {
      margin-top: 8px;
    }

    /* Fixed Input Area */
    .chat-input {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 100;
      background: rgba(255,255,255,.95);
      backdrop-filter: blur(10px);
      border-top: 1px solid #eee;
      padding: 12px 16px calc(12px + var(--safe-bottom));
    }
    .chat-form {
      display: flex;
      gap: 10px;
      align-items: center;
    }
    .chat-text {
      flex: 1;
      padding: 12px 16px;
      border: 1px solid #ddd;
      border-radius: 24px;
      font-size: 16px;
      background: #fff;
      outline: none;
      -webkit-appearance: none;
    }
    .chat-text:focus {
      border-color: var(--hermes);
    }
    .chat-send {
      width: 48px;
      height: 48px;
      border: none;
      border-radius: 50%;
      background: var(--hermes);
      color: #fff;
      font-size: 20px;
      cursor: pointer;
      touch-action: manipulation;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .chat-send:active {
      transform: scale(0.95);
    }

    /* Empty State */
    .chat-empty {
      text-align: center;
      color: var(--muted);
      padding: 40px 20px;
    }
    .chat-empty-icon {
      font-size: 48px;
      margin-bottom: 12px;
    }
  </style>
</head>
<body>
  <!-- Fixed Header -->
  <header class="chat-header">
    <div class="chat-header-left">
      <a class="chat-back" href="/">←</a>
      <span class="chat-title">🤵 <%= @modes.find{|x| x[:key]==@mode}&.dig(:label) || "秘書" %></span>
    </div>
    <span class="chat-badge">mode <%= @mode %></span>
  </header>

  <!-- Scrollable Chat Area (newest first with flex-direction: column-reverse) -->
  <div class="chat-messages">
    <% if @messages.empty? %>
      <div class="chat-empty">
        <div class="chat-empty-icon">🤵</div>
        <p>メッセージを入力してください</p>
      </div>
    <% else %>
      <% @messages.reverse.each do |msg| %>
        <div class="bubble <%= msg.role %>">
          <div class="bubble-role"><%= msg.role == "user" ? "You" : "秘書" %></div>
          <div class="bubble-text"><%= simple_format(h(msg.content)) %></div>
        </div>
      <% end %>
    <% end %>
  </div>

  <!-- Fixed Input Area -->
  <div class="chat-input">
    <%= form_with url: "/hub/send", method: :post, local: true, class: "chat-form" do %>
      <input type="hidden" name="mode" value="<%= @mode %>">
      <input class="chat-text" name="text" placeholder="メッセージを入力..." autocomplete="off" autofocus>
      <button class="chat-send" type="submit">➤</button>
    <% end %>
  </div>

  <script>
    // Auto-scroll to top (newest) on load
    document.addEventListener('DOMContentLoaded', function() {
      const messages = document.querySelector('.chat-messages');
      if (messages) {
        messages.scrollTop = 0;
      }
    });
  </script>
</body>
</html>
